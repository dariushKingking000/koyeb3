name: Remote Browser Controller
on: 
  workflow_dispatch:

permissions:
  contents: write

jobs:
  browser:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Patchright
        run: |
          pip install patchright
          patchright install chromium
          pip install requests

      - name: Run Remote Browser Server
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cat > remote_browser.py << 'EOF'
          import requests
          import time
          import datetime
          import os
          import base64
          import sys
          import uuid
          import random
          import socket
          from patchright.sync_api import sync_playwright

          def debug_print(msg):
              print(f"[{datetime.datetime.now().strftime('%H:%M:%S')}] {msg}")
              sys.stdout.flush()

          TARGET_REPO_OWNER = "dariushKingking000"
          TARGET_REPO_NAME = "koyeb3"
          FILE_PATH = "c.txt"
          FRAME_FOLDER = "frames"
          GITHUB_TOKEN = os.getenv('GITHUB_TOKEN')

          headers = {
              'Authorization': f'token {GITHUB_TOKEN}',
              'Accept': 'application/vnd.github.v3+json'
          }

          # IP های گوگل (بایپس DNS)
          GOOGLE_IPS = [
              "142.250.185.46",   # google.com
              "172.217.168.46",   # google.com
              "216.58.214.46",    # google.com
          ]

          def upload_frame(frame_data, description=""):
              unique_id = f"{datetime.datetime.now().strftime('%Y%m%d_%H%M%S')}_{uuid.uuid4().hex[:8]}"
              file_path = f"{FRAME_FOLDER}/frame_{unique_id}.png"
              content = base64.b64encode(frame_data).decode('utf-8')
              url = f"https://api.github.com/repos/{TARGET_REPO_OWNER}/{TARGET_REPO_NAME}/contents/{file_path}"
              data = {"message": f"📸 {description}", "content": content, "branch": "main"}
              put_response = requests.put(url, headers=headers, json=data)
              if put_response.status_code in [200, 201]:
                  debug_print(f"✅ فریم آپلود شد")
                  return True
              return False

          def get_c_txt_content():
              url = f"https://api.github.com/repos/{TARGET_REPO_OWNER}/{TARGET_REPO_NAME}/contents/{FILE_PATH}"
              try:
                  response = requests.get(url, headers=headers)
                  if response.status_code == 200:
                      content = response.json().get('content', '')
                      if content:
                          decoded = base64.b64decode(content).decode('utf-8')
                          return decoded.strip()
                  return None
              except Exception as e:
                  debug_print(f"❌ خطا: {e}")
                  return None

          def clear_c_txt():
              url = f"https://api.github.com/repos/{TARGET_REPO_OWNER}/{TARGET_REPO_NAME}/contents/{FILE_PATH}"
              response = requests.get(url, headers=headers)
              sha = response.json().get('sha') if response.status_code == 200 else None
              content_b64 = base64.b64encode(b"# waiting").decode('utf-8')
              data = {"message": "🧹 Clear", "content": content_b64, "branch": "main"}
              if sha:
                  data["sha"] = sha
              requests.put(url, headers=headers, json=data)
              debug_print("🗑️ پاک شد")

          def execute_command(page, command):
              cmd_lower = command.lower().strip()

              if cmd_lower.startswith('click '):
                  parts = cmd_lower.split()
                  if len(parts) == 3:
                      try:
                          x, y = int(parts[1]), int(parts[2])
                          debug_print(f"🖱️ کلیک ({x}, {y})")
                          page.mouse.click(x, y)
                          return True
                      except ValueError:
                          debug_print(f"⚠️ مختصات نامعتبر")
                          return False
                  else:
                      debug_print(f"⚠️ فرمت click نامعتبر")
                      return False

              elif cmd_lower == "enter":
                  debug_print(f"⏎ Enter")
                  page.keyboard.press("Enter")
                  return True

              elif cmd_lower == "#refresh" or cmd_lower == "refresh":
                  debug_print(f"🔄 فریم تازه")
                  return True

              else:
                  debug_print(f"⌨️ تایپ: {command}")
                  for ch in command:
                      page.keyboard.type(ch, delay=random.randint(30, 100))
                      time.sleep(random.uniform(0.02, 0.08))
                  return True

          def main():
              debug_print("🚀 شروع با Patchright")
              debug_print("📋 منتظر دستورات...")

              last_command = None

              # تست اتصال DNS
              debug_print("🔍 تست اتصال به اینترنت...")
              try:
                  socket.gethostbyname('google.com')
                  debug_print("✅ DNS کار می‌کند")
              except:
                  debug_print("⚠️ DNS مشکل دارد، استفاده از IP مستقیم")

              with sync_playwright() as p:
                  browser = p.chromium.launch(
                      headless=True,
                      args=[
                          '--no-sandbox',
                          '--disable-dev-shm-usage',
                          '--disable-setuid-sandbox',
                          '--disable-gpu',
                          '--disable-software-rasterizer',
                          '--disable-features=VizDisplayCompositor',
                          '--dns-prefetch-disable',  # غیرفعال کردن DNS prefetch
                      ]
                  )
                  
                  context = browser.new_context(
                      viewport={'width': 1280, 'height': 720},
                      user_agent='Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/120.0.0.0 Safari/537.36',
                      ignore_https_errors=True,  # مهم!
                  )
                  
                  page = context.new_page()
                  
                  # مخفی کردن webdriver
                  page.add_init_script("""
                      Object.defineProperty(navigator, 'webdriver', {get: () => undefined});
                  """)
                  
                  page.set_default_timeout(60000)
                  
                  debug_print("🌐 رفتن به گوگل...")
                  
                  # تلاش با IP مستقیم
                  success = False
                  for ip in GOOGLE_IPS:
                      try:
                          debug_print(f"🔄 تلاش با IP: {ip}")
                          page.goto(f'https://{ip}', wait_until='domcontentloaded', timeout=30000)
                          success = True
                          debug_print(f"✅ با IP {ip} باز شد")
                          break
                      except Exception as e:
                          debug_print(f"⚠️ IP {ip} جواب نداد: {e}")
                  
                  if not success:
                      debug_print("❌ هیچ IP جواب نداد، در حال بسته شدن...")
                      return
                  
                  time.sleep(2)
                  debug_print("✅ گوگل باز شد")

                  screenshot = page.screenshot(type='png')
                  upload_frame(screenshot, "initial")

                  while True:
                      try:
                          content = get_c_txt_content()

                          if content and content != last_command and not content.startswith('#'):
                              debug_print(f"📖 دستور: {content}")

                              if execute_command(page, content):
                                  last_command = content
                                  time.sleep(0.5)
                                  screenshot = page.screenshot(type='png')
                                  upload_frame(screenshot, "after")
                                  clear_c_txt()
                              else:
                                  clear_c_txt()

                          time.sleep(1)

                      except Exception as e:
                          debug_print(f"💥 خطا: {e}")
                          time.sleep(5)

          if __name__ == "__main__":
              main()
          EOF

          python3 remote_browser.py
