name: Google Screenshot to Repo
on:9ع9عorkflow_dispatch:
  repos9ry_dispatch:
    t8es: [flask-alive]

permissions:
  c8899ontents: write
899ith:
      78  tokمتتختختخهتen: ${{ secrets.GITHUB_TOKEN }}
   098     fetch-depth: 0  # کل history رو بگیر
    
اses: acti98ons/setup-python@v5
      with:
        pyt8ا
    - name: Install Playwright
  ه run: |
  د      pip install playwright pillow
  هد    playwright inst
        python -c "
        from playwright.sync_api import sync_playwright
        import datetime
